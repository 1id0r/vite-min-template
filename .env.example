# API Configuration Template
# Copy this file to .env and adjust values for your environment

# Base URL for the API (Where /config is fetched from)
# If your real API is at https://api.mysystem.com, set it here.
# Defaults to http://127.0.0.1:8000 if not set.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Optional: Specific route for fetching the tree
# If the tree API is different or absolute, your can set it here.
# Example: https://tree-api.mysystem.com/get-tree
# VITE_TREE_ROUTE=/tree

# Optional: Token for tree API
# VITE_TREE_APP_TOKEN=your-token-here

# Optional: Use static configuration (Bypass /config endpoint)
# Set to 'true' if your backend does not provide configuration
# VITE_USE_STATIC_CONFIG=true
